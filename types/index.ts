export type SiteConfig = {
  name: string;
  tagline: string;
  description: string;
};

export type FAQProps = {
  question: string;
  answer: string;
  value: string;
};

export type Project = {
  url: string;
  title: string;
  description: string;
  image: string;
}
